import 'package:flutter/material.dart';

class ViagensScreen extends StatelessWidget {
  const ViagensScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Viagens Screen'),
    );
  }
}
